# AHFL-Masking 1.1 — Batch Processor GPU Requirements
#
# IMPORTANT: Install PaddlePaddle GPU BEFORE running pip install -r on this file.
# It is NOT on PyPI — use the official PaddlePaddle index matching your CUDA version:
#
#   CUDA 11.8:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
#   CUDA 12.6:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#   CUDA 12.9:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
#   CUDA 13.0:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu130/
#
# The Dockerfile handles this automatically (line 30). This note is for manual installs.

python-dotenv==1.0.1
python-multipart==0.0.9
pydantic==2.7.0

# ML / Vision — GPU Variants
# Installed in Dockerfile from Paddle official cu121 index.
# Keep commented here to avoid duplicate installs/resolution conflicts.
# paddlepaddle-gpu==3.3.0
paddleocr==3.4.0
ultralytics==8.3.235

# PyTorch + CUDA (for YOLO with GPU acceleration)
# Installed in Dockerfile from PyTorch cu121 index.
# Keep commented here to avoid duplicate installs/resolution conflicts.
# torch==2.1.2+cu121
# torchvision==0.16.2+cu121

# OpenCV (GPU acceleration optional)
opencv-python-headless==4.9.0.80

# Numerical computing
numpy==1.26.4
Pillow==10.4.0
## Pin protobuf to avoid ML wheel binary incompatibility.
# Vendor wheels: paddlepaddle, paddleocr, torch.
# Do not change without testing native wheel compatibility.
# protobuf==3.20.3

# PDF handling
pdf2image==1.17.0
img2pdf==0.5.1
PyPDF2==3.0.1
patool==3.1.3

# OCR support
pytesseract==0.3.13

# DB
boto3>=1.34.0
