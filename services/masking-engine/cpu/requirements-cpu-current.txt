# AHFL-Masking 1.1 — Masking Engine CPU Current Stack
# Python: 3.10
# PaddleOCR: 3.4.0
# PaddlePaddle (CPU): 3.3.0
# paddlepaddle (CPU) and torch/torchvision are installed separately in Dockerfile.
# Keep them commented here to avoid duplicate installs/resolution conflicts.
#
#   paddlepaddle==3.3.0          (CPU, PyPI)
#   torch==2.1.2                 (CPU, PyPI)
#   torchvision==0.16.2          (CPU, PyPI)

# FastAPI + Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.7.0
pydantic-settings==2.2.0

# ML / Vision
# PaddleOCR (CPU-accelerated via paddlepaddle[cpu])
paddleocr==3.4.0
# paddlepaddle==3.3.0
#
# PyTorch CPU — installed in Dockerfile.
# Keep commented here to avoid duplicate installs/resolution conflicts.
# torch==2.1.2
# torchvision==0.16.2

# Ultralytics YOLO (uses PyTorch automatically)
ultralytics==8.3.235

opencv-python-headless==4.9.0.80
numpy==1.26.4
Pillow==10.4.0
## Pin protobuf to avoid ML wheel binary incompatibility.
# Vendor wheels: paddlepaddle, paddleocr, torch.
# Do not change without testing native wheel compatibility.
# protobuf==3.20.3

# PDF handling
pdf2image==1.17.0
img2pdf==0.5.1
PyPDF2==3.0.1

# OCR support
pytesseract==0.3.13

# Monitoring + Logging
python-json-logger==2.0.7
psutil==5.9.8
