# AHFL-Masking 1.1 — GPU Optimized Requirements
#
# IMPORTANT: Install PaddlePaddle GPU BEFORE running pip install -r on this file.
# It is NOT on PyPI — use the official PaddlePaddle index matching your CUDA version:
#
#   CUDA 11.8:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
#   CUDA 12.6:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#   CUDA 12.9:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu129/
#   CUDA 13.0:  pip install paddlepaddle-gpu==3.3.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu130/
#
# paddlepaddle-gpu==2.6.2 (old PyPI version) only supports CUDA 10.2/11.x — do NOT use it.

# FastAPI + Server
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.7.0
pydantic-settings==2.2.0

# ML / Vision — GPU Variants
# PaddleOCR (GPU-accelerated via paddlepaddle[gpu])
paddleocr==3.4.0

# PyTorch + CUDA — installed separately in Dockerfile from PyTorch index
# torch==2.1.2+cu121
# torchvision==0.16.2+cu121

# Ultralytics YOLO (uses PyTorch GPU support automatically)
ultralytics==8.3.235

# OpenCV (GPU acceleration optional)
opencv-python-headless==4.9.0.80

# Numerical computing
numpy==1.26.4
Pillow==10.4.0
protobuf==3.20.3

# ONNX Runtime disabled due CUDA/provider conflicts in current stack.
# Re-enable only if we move inference path to ONNX end-to-end.
# onnx==1.15.0
# onnxruntime-gpu==1.17.0

# Optional: TensorRT for YOLO optimization (comment out if not needed)
# Note: TensorRT requires additional setup - see GPU_DEPLOYMENT_GUIDE.md
# tensorrt>=8.6.0

# PDF handling
pdf2image==1.17.0
img2pdf==0.5.1
PyPDF2==3.0.1

# OCR support
pytesseract==0.3.13

# Monitoring + Logging
python-json-logger==2.0.7
psutil==5.9.8
